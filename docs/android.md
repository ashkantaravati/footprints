# Android

The Android app is a Kotlin Android Studio project. It currently renders the PWA in a WebView, persists cookies for sessions, requests notification permission on Android 13+, declares a deep-link scheme, and includes a notification channel bridge for native notification work.

Update `android/app/src/main/res/values/strings.xml` so `footprints_url` points to your hosted PWA.
